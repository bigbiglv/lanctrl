# LanCtrl Global AI Guidelines

## 基本交互规范 / General AI Constraints
- **语言强制**：作为 AI，无论用户如何提问，您必须始终**使用中文且仅用中文**作答。代码中的注释、说明文档也必须使用中文。
- **项目认知**：本项目 `LanCtrl` 是一个“同一局域网内多台 Windows 电脑轻量化控制系统”。
- **单模块边界**：本项目分为两大核心：PC 端 (`lanctrl-pc`) 和 移动端 (`lanctrl-mobile`)。通常情况下修改不应该同时大面积越界破坏二者的接口。
- **稳定性优先**：“快速上线不是第一优先级”，第一优先级永远是架构扩展性和长期维护性。不要采用拼凑或妥协性能的代码。

## 代码提交流程与规范
- 修改前必须明确阅读相关 `requirements*.md` 文档。
- 生成的代码必须始终符合社区标准并在保证性能的前提下尽可能的解耦。
